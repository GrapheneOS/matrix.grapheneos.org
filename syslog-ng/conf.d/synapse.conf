source s_synapse_main {
    unix-dgram("/run/synapse-main-log" group("synapse") perm(0660));
};
source s_synapse_background {
    unix-dgram("/run/synapse-background-log" group("synapse") perm(0660));
};
source s_synapse_client_reader {
    unix-dgram("/run/synapse-client_reader-log" group("synapse") perm(0660));
};
source s_synapse_federation_reader {
    unix-dgram("/run/synapse-federation_reader-log" group("synapse") perm(0660));
};
source s_synapse_federation_sender1 {
    unix-dgram("/run/synapse-federation_sender1-log" group("synapse") perm(0660));
};
source s_synapse_federation_sender2 {
    unix-dgram("/run/synapse-federation_sender2-log" group("synapse") perm(0660));
};
source s_synapse_federation_sender3 {
    unix-dgram("/run/synapse-federation_sender3-log" group("synapse") perm(0660));
};
source s_synapse_federation_sender4 {
    unix-dgram("/run/synapse-federation_sender4-log" group("synapse") perm(0660));
};
source s_synapse_media_repository {
    unix-dgram("/run/synapse-media_repository-log" group("synapse") perm(0660));
};
source s_synapse_pusher {
    unix-dgram("/run/synapse-pusher-log" group("synapse") perm(0660));
};
source s_synapse_stream_writer {
    unix-dgram("/run/synapse-stream_writer-log" group("synapse") perm(0660));
};
source s_synapse_sync {
    unix-dgram("/run/synapse-sync-log" group("synapse") perm(0660));
};

destination d_synapse_main {
    file("/var/log/synapse/main.log" template("${ISODATE} ${LEVEL} ${MESSAGE}\n"));
};
destination d_synapse_background {
    file("/var/log/synapse/background.log" template("${ISODATE} ${LEVEL} ${MESSAGE}\n"));
};
destination d_synapse_client_reader {
    file("/var/log/synapse/client_reader.log" template("${ISODATE} ${LEVEL} ${MESSAGE}\n"));
};
destination d_synapse_federation_reader {
    file("/var/log/synapse/federation_reader.log" template("${ISODATE} ${LEVEL} ${MESSAGE}\n"));
};
destination d_synapse_federation_sender1 {
    file("/var/log/synapse/federation_sender1.log" template("${ISODATE} ${LEVEL} ${MESSAGE}\n"));
};
destination d_synapse_federation_sender2 {
    file("/var/log/synapse/federation_sender2.log" template("${ISODATE} ${LEVEL} ${MESSAGE}\n"));
};
destination d_synapse_federation_sender3 {
    file("/var/log/synapse/federation_sender3.log" template("${ISODATE} ${LEVEL} ${MESSAGE}\n"));
};
destination d_synapse_federation_sender4 {
    file("/var/log/synapse/federation_sender4.log" template("${ISODATE} ${LEVEL} ${MESSAGE}\n"));
};
destination d_synapse_media_repository {
    file("/var/log/synapse/media_repository.log" template("${ISODATE} ${LEVEL} ${MESSAGE}\n"));
};
destination d_synapse_pusher {
    file("/var/log/synapse/pusher.log" template("${ISODATE} ${LEVEL} ${MESSAGE}\n"));
};
destination d_synapse_stream_writer {
    file("/var/log/synapse/stream_writer.log" template("${ISODATE} ${LEVEL} ${MESSAGE}\n"));
};
destination d_synapse_sync {
    file("/var/log/synapse/sync.log" template("${ISODATE} ${LEVEL} ${MESSAGE}\n"));
};

log {
    source(s_synapse_main);
    destination(d_synapse_main);
};
log {
    source(s_synapse_background);
    destination(d_synapse_background);
};
log {
    source(s_synapse_client_reader);
    destination(d_synapse_client_reader);
};
log {
    source(s_synapse_federation_reader);
    destination(d_synapse_federation_reader);
};
log {
    source(s_synapse_federation_sender1);
    destination(d_synapse_federation_sender1);
};
log {
    source(s_synapse_federation_sender2);
    destination(d_synapse_federation_sender2);
};
log {
    source(s_synapse_federation_sender3);
    destination(d_synapse_federation_sender3);
};
log {
    source(s_synapse_federation_sender4);
    destination(d_synapse_federation_sender4);
};
log {
    source(s_synapse_media_repository);
    destination(d_synapse_media_repository);
};
log {
    source(s_synapse_pusher);
    destination(d_synapse_pusher);
};
log {
    source(s_synapse_stream_writer);
    destination(d_synapse_stream_writer);
};
log {
    source(s_synapse_sync);
    destination(d_synapse_sync);
};
